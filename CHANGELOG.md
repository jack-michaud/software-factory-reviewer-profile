# Changelog

## Unreleased

- Restructured `SOUL.md` into an actual progressive-disclosure root map with detailed conditional role doctrine moved to `references/role-operating-guidance.md` and distribution-managed reference wiring.
- Remediated the remote-sprite PM seed example so copyable tasks leave forced startup skills unset by default and use `remote-sprite-development` only after exact target-profile loadability is verified, otherwise inlining the contract.
- Added approval-gate dependency doctrine: approval/decision gates that unblock blocked seeds must be unparented siblings or parent/unblockers of future execution work, not children of the blocked seed.

## 0.1.0

Initial prototype role distribution.
