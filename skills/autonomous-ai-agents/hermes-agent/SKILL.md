---
name: hermes-agent
description: Public Hermes Agent profile distribution install/update reference.
version: 0.1.0
---
# Hermes Agent Profile Distribution

Install a local profile distribution with `hermes profile install /path/to/distribution --name disposable-name --yes`. For generated public repositories, the repository root must contain `distribution.yaml`.
